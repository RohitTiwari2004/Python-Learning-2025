#Library : collection of modules and packages.
import math 
A = 16
print(math.sqrt(A))

#lets suppose you want to get the output in integer not in decimal point at that case we can use int function in the print statement

import math 
B = 25
print(int(math.sqrt(B)))

#important specific function from lib
from math import factorial 
C = 4
print(factorial(C))

#installed new modules/library
#pip install <library_name>


from math import sqrt
a = 36
print(int(sqrt(a)))


