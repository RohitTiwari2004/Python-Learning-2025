# To perform any operation  (read/write) on a file, you first need to open the file using Python's open() funciton.
# syntax to open a file in python 

#File_Object = open('filename', 'mode')
#Mode = 'Mode' in which the file is opened. (read, write, append)
# 'r' Read = Read defaults mode . Opens the file for reading
# 'w' write .Opens the file for writing (if file doesn't exist, it creates one)
#append - 'a' Opens the file for appending (if file doesnot exist, it creates one)
#'rb'/'wb' : Read/Write  in binary mode.

